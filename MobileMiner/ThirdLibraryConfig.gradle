ext {
    JUNIT_VERSION = JUNIT_VERSION as String
    APPCOMPAT_VERSION = APPCOMPAT_VERSION as String
    OK_HTTP3_VERSION = OK_HTTP3_VERSION as String
    SUPPORT_V4_VERSION = SUPPORT_V4_VERSION as String
    TEST_RUNNER_VERSION = TEST_RUNNER_VERSION as String
}